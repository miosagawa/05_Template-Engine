import { OnInit } from '@angular/core';
import { NavTab } from "./nav-tab";
import { BottomBarService } from "../bottom-bar.service";
export declare class BottomNavItemComponent implements OnInit {
    bottomNavService: BottomBarService;
    tab: NavTab;
    constructor(bottomNavService: BottomBarService);
    ngOnInit(): void;
    /**
     * Method that will change of active tab
     * @param tab
     */
    private changeState(tab);
}

import { OnInit } from '@angular/core';
import { NavTab } from "../bottom-nav-item/nav-tab";
import { BottomBarService } from "../bottom-bar.service";
export declare class BottomBarComponent implements OnInit {
    private bottonNavService;
    /**
     * Group of NavTab items that will be use for the bottom bar
     */
    tabs: NavTab[];
    initialColor: string;
    showTextInCaseMore3Elements: boolean;
    /**
     * Variable that will be use for set the color of the background
     */
    color: String;
    constructor(bottonNavService: BottomBarService);
    ngOnInit(): void;
}
